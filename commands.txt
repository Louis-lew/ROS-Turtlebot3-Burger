
#run for first time
mkdir ~/webot_ws00/src -p
cd webot_ws00/src
catkin_create_pkg mywebots std_msgs rospy roscpp
cd ..
#include webots_ros-4.0.1
catkin_make

----------------------------------------------------
#run for each run (include webot ros)
cd ~/webot_ws00/
source /opt/ros/noetic/setup.bash
export TURTLEBOT3_MODEL=burger
catkin_make
source devel/setup.bash
roscd mywebots
export WEBOTS_HOME=/usr/local/webots
clear

#edit launch files(pkg=mywebots,controller = extern)
roslaunch mywebots webots_ros_python.launch

----------------------------------------------------
#run custom package-python file (teleop)
rosrun mywebots turtlebot3_teleop_key.py

----------------------------------------------------
rosrun gmapping slam_gmapping scan:=/scan _xmax:=30 _xmax:=30 _xmin:=-30 _ymax:=30 _ymin:=-30 _delta:=0.2

rosrun gmapping slam_gmapping scan:=base_scan _odom_frame:=odom_combined

----------------------------------------------------
#manual add tf and map for now
rosrun rviz rviz

#or
export TURTLEBOT3_MODEL=burger
roslaunch turtlebot3_slam turtlebot3_slam.launch slam_methods:=gmapping

